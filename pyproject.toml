[project]
name = "pokemon-dlt-dbt-pipeline"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["pokemon-dlt-pipeline", "pokemon-dbt-pipeline"]
